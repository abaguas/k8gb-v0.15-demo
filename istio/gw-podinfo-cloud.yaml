---
apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: podinfo-cloud
  namespace: istio-ingress
spec:
  selector:
    app: istio-ingress
  servers:
    - hosts:
        - podinfo.cloud.k8gb.io
      port:
        name: http
        number: 8080
        protocol: http
